import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { CustomerPortalMember } from "../models/components/customerportalmember.js";
import { CustomerPortalMemberCreate } from "../models/components/customerportalmembercreate.js";
import { CustomerPortalMembersListMembersRequest, CustomerPortalMembersListMembersResponse } from "../models/operations/customerportalmemberslistmembers.js";
import { CustomerPortalMembersRemoveMemberRequest } from "../models/operations/customerportalmembersremovemember.js";
import { CustomerPortalMembersUpdateMemberRequest } from "../models/operations/customerportalmembersupdatemember.js";
import { PageIterator } from "../types/operations.js";
export declare class PolarMembers extends ClientSDK {
    /**
     * List Members
     *
     * @remarks
     * List all members of the customer's team.
     *
     * Only available to owners and billing managers of team customers.
     */
    listMembers(request: CustomerPortalMembersListMembersRequest, options?: RequestOptions): Promise<PageIterator<CustomerPortalMembersListMembersResponse, {
        page: number;
    }>>;
    /**
     * Add Member
     *
     * @remarks
     * Add a new member to the customer's team.
     *
     * Only available to owners and billing managers of team customers.
     *
     * Rules:
     * - Cannot add a member with the owner role (there must be exactly one owner)
     * - If a member with this email already exists, the existing member is returned
     */
    addMember(request: CustomerPortalMemberCreate, options?: RequestOptions): Promise<CustomerPortalMember>;
    /**
     * Remove Member
     *
     * @remarks
     * Remove a member from the team.
     *
     * Only available to owners and billing managers of team customers.
     *
     * Rules:
     * - Cannot remove yourself
     * - Cannot remove the only owner
     */
    removeMember(request: CustomerPortalMembersRemoveMemberRequest, options?: RequestOptions): Promise<void>;
    /**
     * Update Member
     *
     * @remarks
     * Update a member's role.
     *
     * Only available to owners and billing managers of team customers.
     *
     * Rules:
     * - Cannot modify your own role (to prevent self-demotion)
     * - Customer must have exactly one owner at all times
     */
    updateMember(request: CustomerPortalMembersUpdateMemberRequest, options?: RequestOptions): Promise<CustomerPortalMember>;
}
//# sourceMappingURL=polarmembers.d.ts.map